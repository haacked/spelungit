# Minimal requirements for CI testing
# Only includes dependencies needed for basic functionality and testing

# Core MCP dependencies
mcp>=1.0.0

# Basic Python dependencies
numpy>=1.24.0

# Testing framework (minimal)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Code quality tools
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0

# Security scanning
bandit>=1.7.5
safety>=2.3.0

# Type stubs
types-requests>=2.31.0

# Note: Excludes heavy ML dependencies like:
# - sentence-transformers (887.9 MB)
# - torch (several GB with CUDA)
# - scikit-learn
# These are only needed for full embedding functionality, not basic commands
